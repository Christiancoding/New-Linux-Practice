# Core Dependencies
flask==3.0.0
jinja2==3.1.2

# Database & ORM
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL driver
pymysql==1.1.0  # MySQL driver (optional)

# Database Connection Pooling
sqlalchemy-pool==1.3.0

# Data Validation & Serialization
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0

# Configuration Management
python-dotenv==1.0.0

# Utilities
click==8.1.7  # For CLI commands
colorama==0.4.6  # For colored terminal output

# Development & Testing (optional)
pytest==7.4.3
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0

# Security
bcrypt==4.1.2  # For password hashing when auth is added
cryptography==41.0.8  # For general encryption needs

# Performance & Caching (for future use)
redis==5.0.1
celery==5.3.4

# Web Server (for production)
gunicorn==21.2.0
waitress==2.1.2  # Alternative to gunicorn for Windows

# API Documentation (for future REST API)
flask-restx==1.3.0
apispec==6.3.0

# Migration from current JSON structure
jsonschema==4.20.0  # For validating existing JSON data during migration